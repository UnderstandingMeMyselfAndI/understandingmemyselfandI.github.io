# Behavior

- Be concise. Do not explain the code unless I ask.
- If you change a file, always run the associated test suite before finishing.Be concise. Don't apologize. Focus on code
  generation

# Coding Standards

- always use css selectors and an external .scss
- when creating components in reactjs always crate the .scss file next to the component using the component name and
  .scss extension

# React Best Practices

- Use Functional Components and Hooks (useState, useEffect, etc.).
- Avoid Class Components unless specifically asked.
- Always ensure components are exported as 'default'
- Don't hallucinate imports: Check the file tree before importing
